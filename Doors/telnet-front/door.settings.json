{
  "command": "TELNET-FRONT",
  "settings": [
    {
      "key": "bbsAddress",
      "label": "Board address",
      "type": "string",
      "default": "",
      "help": "What users type to reach this board - bbs.example.net, or an IP. Blank falls back to BBS_IP, then to the machine's own address, which inside a container is a private one nobody can dial."
    },
    {
      "key": "maxNodes",
      "label": "Nodes shown",
      "type": "number",
      "default": 255,
      "min": 1,
      "max": 255,
      "help": "How many nodes the login screen looks at. It prints the ones in use, so a high number costs nothing; set it to your board's MAX_NODES."
    }
  ]
}
