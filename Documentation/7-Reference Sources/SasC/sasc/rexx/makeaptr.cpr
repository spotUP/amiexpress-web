/* */
address 'COMMAND'
'version >nil: exec.library 36 '
lrc = rc

if (rc = 0) then
  check = addlib('rexxsupport.library',0,-30,0) 

address

parse arg addr
if (~datatype(addr,'x')) then
   do
   'd "Invalid hex number' arg '"'
   exit(0)
   end
'd "' c2x(d2c(c2d(x2c(addr))*4,4)) '"'
exit(0)