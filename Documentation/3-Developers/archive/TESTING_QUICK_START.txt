================================================================================
  TESTING QUICK START - Always Use This When Debugging
================================================================================

📝 MASTER TEST SCRIPT: /test-ga-command.js

🔄 TO TEST ANY FEATURE:

  1. Copy the script:
     cp test-ga-command.js test-my-feature.js

  2. Edit the command:
     # Change "GA" to your command (e.g., "B", "F", "DOORS")
     vim test-my-feature.js

  3. Run it:
     node test-my-feature.js

  4. Monitor logs (in separate terminal):
     tail -f /tmp/backend.log | grep -E "MyFeature|ERROR|🔊"

✅ CORRECT LOGIN SEQUENCE (never change this):
   A → Enter → sysop → Enter → sysop → Enter → Enter → Enter → [COMMAND]

📚 FULL DOCUMENTATION:
   /Docs/TESTING_WITH_PUPPETEER.md
   /Docs/GA_DOOR_INFINITE_LOOP_FIX.md (current door issue analysis)

🐛 CURRENT ISSUE:
   GA door stuck in infinite loop at PC=0x1022
   - Register A1 = 0 (should point to DATA segment)
   - Loop copies garbage data forever
   - Never reaches XIM output commands
   - Need to fix HunkLoader or door initialization

================================================================================
