# Environment variables for AmiExpress Web Backend
PORT=3001
NODE_ENV=production

# PostgreSQL database URL (for Vercel/Render.com deployment)
DATABASE_URL=postgresql://username:password@hostname:5432/database_name
# Alternative for Vercel
POSTGRES_URL=postgresql://username:password@hostname:5432/database_name

# Redis URL for session storage (optional - falls back to in-memory if not set)
# Get free Redis from: https://upstash.com or https://redis.com/cloud
# Example: redis://default:password@redis-12345.upstash.io:6379
REDIS_URL=redis://localhost:6379

# Security
JWT_SECRET=your-jwt-secret-here

# Session Configuration
SESSION_TIMEOUT=3600000
MAX_SESSIONS=100

# Default Sysop Credentials
SYSOP_USERNAME=sysop
SYSOP_PASSWORD=change-this-password