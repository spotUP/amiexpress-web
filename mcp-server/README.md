# MCP Server Documentation
This helper server loads AmiExpress sources for reference. For current documentation, start at `Documentation/README.md`—it maps the audience-based summaries and the `archive/` directories.

Test automation lives in `Scripts/` now; see `Scripts/README.md` for where each test harness resides (`dev/`, `backend/`, `backend-dev/`, `emulation/`, `legacy/`).
