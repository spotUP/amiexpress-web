CRITICAL: Backend restart required for gmaster changes

The following fixes are built but NOT loaded yet:
1. Screen clear delay for modem speeds (ghosting fix)
2. Animation centering (hard drop, COOL/REGRET, combo, T-Spin)
3. Missing 'cool' particle preset
4. Menu ESC handler

You MUST restart the backend to load the new door bundle:

Step 1: Kill backend
--------
cd /Users/spot/Code/amiexpress-web
./dev/scripts/kill-servers.sh

Step 2: Verify no processes running
--------
ps aux | grep -E "node.*backend" | grep -v grep

If you see any node processes, kill them manually:
pkill -f "start-servers"
pkill -f "backend/dist"

Step 3: Start backend
--------
./dev/scripts/start-servers.sh

Step 4: Wait for "Backend ready" message
--------
Look for:
- "Backend ready"
- "Door watcher enabled"

Step 5: Test GMASTER
--------
Connect via telnet/SSH and run GMASTER

What to verify:
- No livechat ghosting when door starts (at modem speed)
- Hard drop animation is CENTERED (not left-aligned)
- Grade-up banner is CENTERED
- COOL banner is CENTERED with cyan/white/green particles
- Combo flashes are CENTERED
- T-Spin flashes are CENTERED
- ESC in main menu exits cleanly (no lockup)

If fixes still don't work after restart:
- Check backend logs: tail -f logs/backend.log
- Check door bundle loaded: logs should show "Loading door: gmaster"
- Verify bundle timestamp: ls -la web/backend/dist/doors/gmaster.bundle.js
