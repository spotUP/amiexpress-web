# Backup files
*.bak
*.bak[0-9]
*.backup
*.final
*.fix*

# Moira CPU emulator source (keep only compiled artifacts)
src/amiga-emulation/cpu/moira-source/

# Test files (unit/integration live in tests/, so allow tracked suites there)
!tests/**/*.test.ts
!tests/**/*.spec.ts

# Development files
*.dev.ts
*.tmp
